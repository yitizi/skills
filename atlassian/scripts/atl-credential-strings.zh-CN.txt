# Atlassian credential dialog — Chinese strings (UTF-8 no BOM, LF)
# Loaded by atl-credential.ps1 via Get-Content -Encoding UTF8
# Format: KEY=value (one per line, # for comments)
#
# Why external file: PowerShell 5.1 reads .ps1 source as system ANSI (GBK on
# Chinese Windows), which corrupts inline Chinese strings. .NET WinForms
# (UTF-16 internally) renders Chinese correctly when assigned at runtime
# from a UTF-8 file via -Encoding UTF8.

TITLE=Atlassian 配置（Jira + Confluence + Bitbucket）

GROUP_JIRA=Jira
GROUP_CONFLUENCE=Confluence
GROUP_BITBUCKET=Bitbucket（可选）

LABEL_URL=服务器地址：
LABEL_USER=用户名：
LABEL_PASS=密码 / Token：

HINT_JIRA=留空密码 = 保留已有条目（如有）
HINT_CONFLUENCE=与 Jira 同主机时仅写一条 .netrc
HINT_BITBUCKET=v1 通过 Jira 反查 PR/分支/提交；填了等 v2 直连用

FOOTER_LINE1=密码本地保存于 ~/.netrc，不会传给 AI；URL/用户名存于 config.env
FOOTER_LINE2=已配置项的 URL/用户名已自动预填，密码留空 = 复用 .netrc 已有条目
FOOTER_LINE3=Jira / Confluence / Bitbucket 至少配置一项；其余留空跳过

BTN_OK=确定
BTN_CANCEL=取消

ERR_AT_LEAST_ONE=至少需要配置一项（Jira / Confluence / Bitbucket），含 URL 和用户名
ERR_INVALID_URL=URL 格式无效：
ERR_PASSWORD_REQUIRED=必须填写密码（无可复用的现有 .netrc 条目）：
WARN_SAME_HOST_DIFF_USER=同主机但用户名不同；.netrc 仅保留最后写入的那个
